#!/usr/bin/env bash
# Safe compatibility replacement for the old machine-specific cleanup script.
# It removes local build output only. It never kills processes or restarts ToC.

set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")" && pwd)"
cd "$REPO_ROOT"

make clean
echo "Build output removed. No running process was changed."
