#!/usr/bin/env bash
# Compatibility entrypoint. The maintained launcher is startup.sh.

set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")" && pwd)"
echo "The 'startup' compatibility name now delegates to startup.sh." >&2
exec "$REPO_ROOT/startup.sh" "$@"
